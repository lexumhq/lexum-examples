domain Deployment {
  scope { domain }

  state {
    configured: int
    running: int
  }

  goal Healthy {
    target configured + running == 2
  }

  transition Configure {
    slice step {
      configured = 1
    }
  }

  transition Start {
    slice step {
      running = 1
    }
  }
}
